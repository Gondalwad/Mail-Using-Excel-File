<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Select Excel File</title>
<style>
    *{
        box-sizing: border-box;
    }
  body {
    margin: 0;
    padding: 0;
    background: linear-gradient(to bottom, #007bff, #ffffff);
    background-repeat:no-repeat ;
    font-family: Arial, sans-serif; /* Add a fallback font */
  }
  
  .container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh; /* Increase height for better centering */
    text-align: center;
    flex-direction: column; /* Align items vertically */
  }
  
  .file-input {
    display: none;
  }
  
  .file-label {
    padding: 15px 30px; /* Increase padding for better clickability */
    background-color: #ffffff;
    border: 2px solid #007bff;
    color: #007bff;
    cursor: pointer;
    border-radius: 5px;
    font-size: 18px;
    transition: all 0.3s ease;
    margin-bottom: 10px; /* Add some space below the label */
  }
  
  .file-label:hover {
    background-color: #e05d43;
    color: #ffffff;
  }

  .sub{
    padding: 10px 20px; /* Increase padding */
    font-size: larger; /* Use a larger font size */
    background-color: #e05d43;
    cursor: pointer;
    border: none; /* Remove default button border */
    border-radius: 5px;
    transition: background-color 0.3s ease; /* Add transition effect */
  }
  .sub:hover{
    background-color: #ff7f50; /* Darken color on hover */
  }

  #file-name {
    margin-top: 17px; /* Add space above the file name */
    font-size: 16px;
    font-weight: bold;
    color: #666; /* Dim color */
  }

</style>
</head>
<body>

  <div class="container">
    <form action="/hello" method="POST" enctype="multipart/form-data">
        <label for="file" class="file-label">Select Excel File</label>
        <input type="file" id="file" class="file-input" accept=".xlsx,.xls" name='file' required>
        <div id="file-name"></div> <!-- Container for file name -->
        <br>
        <br>
        <button type="submit" class="sub">Submit</button>
    </form>
  </div> 

<script>
  // JavaScript to display selected file name
  const fileInput = document.getElementById('file');
  const fileNameContainer = document.getElementById('file-name');

  fileInput.addEventListener('change', function() {
    if (fileInput.files.length > 0) {
      const fileName = fileInput.files[0].name;
      fileNameContainer.textContent = 'Selected file: ' + fileName;
    } else {
      fileNameContainer.textContent = ''; // Clear file name when no file selected
    }
  });
</script>

</body>
</html>
